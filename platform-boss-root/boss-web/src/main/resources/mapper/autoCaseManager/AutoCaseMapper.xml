<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.boss.platform.dao.autoCaseManager.AutoCaseMapper">
    <resultMap id="BaseResultMap" type="cn.boss.platform.bean.autoManager.AutoCaseBean">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="project_id" jdbcType="INTEGER" property="projectId"/>
        <result column="group_id" jdbcType="INTEGER" property="groupId"/>
        <result column="env_id" jdbcType="INTEGER" property="envId"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="message" jdbcType="VARCHAR" property="message"/>
        <result column="author" jdbcType="VARCHAR" property="author"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <!--添加批用例 -->
    <insert id="addAutoCase" parameterType="cn.boss.platform.bean.autoManager.AutoCaseBean">
        insert into boss_auto_case_tb (project_id,group_id,env_id,message,name,author,create_time,update_time)
        VALUES (#{projectId},#{groupId},#{envId},#{message},#{name},#{author},#{createTime},#{updateTime});
    </insert>

    <!--分页查询批用例 -->
    <select id="getAutoCase" parameterType="map" resultMap="BaseResultMap">
        select * from boss_auto_case_tb where 1=1
        <if test="projectId != null">
            AND project_id = '${projectId}'
        </if>
        <if test="groupId != null">
            AND group_id = '${groupId}'
        </if>
        <if test="envId != null">
            AND env_id = '${envId}'
        </if>
        <if test="id != null">
            AND id = '${id}'
        </if>
        ORDER BY id ASC limit #{startSize},#{pageSize}
    </select>

    <!--分页查询批用例总数 -->
    <select id="getAutoCaseCount" parameterType="map" resultType="java.lang.Integer">
        select count(*) from boss_auto_case_tb where 1=1
        <if test="projectId != null">
            AND project_id = '${projectId}'
        </if>
        <if test="groupId != null">
            AND group_id = '${groupId}'
        </if>
        <if test="envId != null">
            AND env_id = '${envId}'
        </if>
        <if test="id != null">
            AND id = '${id}'
        </if>
    </select>

    <!--分页查询用例，执行用例使用 -->
    <select id="getAutoCases" parameterType="map" resultMap="BaseResultMap">
        select * from boss_auto_case_tb where 1=1
        <if test="projectId != null">
            AND project_id = '${projectId}'
        </if>
        <if test="groupId != null ">
            AND group_id IN
            <foreach item="item" index="index" collection="groupId" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
        <if test="caseId != null ">
            AND id IN
            <foreach item="item" index="index" collection="caseId" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
        ORDER BY id ASC limit #{startSize},#{pageSize}
    </select>


    <select id="getAutoCaseBygroupId" parameterType="java.util.List" resultMap="BaseResultMap">
        select * from boss_auto_case_tb where 1=1 AND group_id IN
        <foreach collection="list"  index ="index" item="item"  open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>



    <!--删除用例 -->
    <delete id="deleteAutoCase" parameterType="java.lang.Integer">
        DELETE FROM boss_auto_case_tb WHERE id = #{id}
    </delete>



    <!--查询单接口 -->
    <select id="getAutoCaseByProjectGroupId" resultMap="BaseResultMap">
        select * from boss_auto_case_tb where 1=1
        <if test="projectId !=null">
            AND project_id = '${projectId}'
        </if>
        <if test="groupId != null ">
            AND group_id = '${groupId}'
        </if>
        ORDER BY id DESC
    </select>


</mapper>